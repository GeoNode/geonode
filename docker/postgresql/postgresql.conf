# Keep the configuration generated by the PostgreSQL image/initdb.
include '/var/lib/postgresql/data/postgresql.conf'

# Keep GeoNode's environment-driven defaults, then allow host overrides.
include_if_exists '/tmp/geonode-postgresql-defaults.conf'
include_dir '/etc/postgresql/conf.d'
