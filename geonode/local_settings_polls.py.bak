# additional settings for polls Django tutorial
# https://docs.djangoproject.com/en/1.7/intro/tutorial01/

POLLS_APPS = (
    'django-polls.polls',
)

# print 'in local_settings_polls.py'

# from local_settings import INSTALLED_APPS
# INSTALLED_APPS += POLLS_APPS

# print 'in local_settings_polls.py, INSTALLED_APPS:', INSTALLED_APPS

# import logging
# for _module in ["django-polls"]:
#     _logger = logging.getLogger(_module)
# 
#     # stream handler logs to standard error stream,
#     # or apache error log when running under mod_wsgi
#     _logger.addHandler(logging.FileHandler("/home/jdoig/logs/polls.log"))
# 
#     # alternatively, you can log to specific file like so:
#     # _logger.addHandler(logging.FileHandler("/path/to/logs/geonode-django.log"))
# 
#     # available levels: DEBUG, INFO, WARNING, ERROR, CRITICAL.
#     # The earlier a level appears in this list, the more output it will produce in the log file.
#     _logger.setLevel(logging.DEBUG)
