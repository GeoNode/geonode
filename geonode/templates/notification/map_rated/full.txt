The user {{ instance.user.username }} rated {{ instance.rating }} the following map:
{{ instance.content_object.title }}, owned by {{ instance.content_object.owner.username }}

You can visit the map's detail page here: http://{{ current_site }}{% url "map_detail" instance.content_object.id %}
