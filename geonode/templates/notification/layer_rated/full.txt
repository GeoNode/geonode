The user {{ instance.user.username }} rated {{ instance.rating }} the following layer:
{{ instance.content_object.title }}, owned by {{ instance.content_object.owner.username }}

You can visit the layer's detail page here: http://{{ current_site }}{% url "layer_detail" instance.content_object.service_typename %}
